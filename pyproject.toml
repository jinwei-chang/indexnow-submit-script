[project]
name = "indexnow-submit-script"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Jin-Wei Chang", email = "jinwei.chang1221@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "httpx>=0.28.1",
    "python-dotenv>=1.2.3",
]

[project.scripts]
indexnow-submit-script = "indexnow_submit_script:main"

[build-system]
requires = ["uv_build>=0.12.7,<0.13.0"]
build-backend = "uv_build"
